<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-OERcA2EqjJCMA+/3y+gxIOqMEjwtxJY7qPCqsdltbNJuaOe923+mo//f6V8Qbsw3" crossorigin="anonymous"></script>
    <style type="text/css">
        textarea {
         background: url(http://i.stack.imgur.com/ynxjD.png) repeat-y;
         width: 800px;
         height: 500px;
         font: normal 14px verdana;
         line-height: 25px;
         padding: 2px 10px;
         border: solid 1px black;
         align-self: center;
         }
        form{
            text-align: center;
            margin-top: 40px;
        }
        h3{
            text-align: center;
            color: darkorange;
            margin-top: 15px;
        }
        .alert{
            margin-left: 250px;
            width: 800px;
        }
        .btn{
          background-color: darkorange;
          color: white;
        }
    </style>
</head>
<body>
    <h3><%= date %></h3>
    <div class="container">
        <% if(status == true ) { %>
          <div class="alert alert-danger alert-dismissible" role="alert">
              <h2>Diary entry already made today.</h2>
              <a href="/" class="alert-link">Main menu</a>
              <button class="btn-close" aria-label="close" data-bs-dismiss="alert"></button>
          </div>
          <% } %>
          <% if(status == false ) { %>
            <div class="alert alert-success alert-dismissible col-7 center block" role="alert">
              <h2>Your entry has been recorded.</h2>
              <a href="/" class="alert-link">Main menu</a>
              <button class="btn-close" aria-label="close" data-bs-dismiss="alert"></button>
            </div>
          <% } %>  
        <form action="/after" method="post">
            <textarea <% if(readonly == true) { %>
                    readonly
                <% } %>  placeholder="Dear diary, " name="entry" cols="30" rows="10"><% if(contents !== null) { %>
                    <%= contents.description %> 
                <% } %></textarea><br>
            <button type="submit" <% if(readonly == true) { %>
                disabled
            <% } %>  class="btn">Submit</button>
        </form>
    </div>
</body>
</html>